class ReelItem {
  final String id, title, subtitle, category, videoUrl, coverImage;
  const ReelItem({
    required this.id,
    required this.title,
    required this.subtitle,
    required this.category,
    required this.videoUrl,
    required this.coverImage,
  });
}
