class NewsArticle {
  final String id, title, subtitle, body, imageUrl, category, author, timeAgo, readTime;
  const NewsArticle({
    required this.id,
    required this.title,
    required this.subtitle,
    required this.body,
    required this.imageUrl,
    required this.category,
    required this.author,
    required this.timeAgo,
    required this.readTime,
  });
}
